# Teamleader GraphQL API

This is a test implementation of a GraphQL layer on top of our current API.

### To run in development mode:

```sh
$ npm start
```

### To build a supported node version inside the `dist` folder

```
$ npm run build
```
